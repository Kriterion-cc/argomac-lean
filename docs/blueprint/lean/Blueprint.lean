/-
This library attaches `@[blueprint]` nodes to the ArgoMAC proofs.
LeanArchitect extracts the nodes with `lake build :blueprint`.
The verified sources in `Construction/` and `Proof/` do not import this library.
-/
import Blueprint.Correctness
import Blueprint.Privacy
import Blueprint.Lamport
